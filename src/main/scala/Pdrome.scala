import stainless.annotation.* 
import stainless.lang.*



object Pdrome {

    // def modifiedLength(x: Int): (Int, Boolean) = {

    // }

    // We can cut of the number for example if 121, we cut off at 1 and by doing % 10 based on length (the zero has to be odd)
    // then we compare if the cut of portion equals the modulo part of the original.
    // Square root is around half right?
    // def isPalindrome(x: Int): Boolean = {
    //     x match
    //         case x if x < 0 => false
    //         case x if x == 0 => true
    //         case _ => 
        
    // } 
}
